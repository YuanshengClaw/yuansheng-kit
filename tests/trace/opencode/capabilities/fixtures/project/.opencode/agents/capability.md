---
description: Capability probe Agent with explicit tool permissions.
mode: primary
model: capability/probe
permission:
  capability_echo: allow
  skill: allow
---

CAPABILITY_AGENT_PROMPT_SENTINEL

Use only the capability probe fixtures and return deterministic results.
